{
  "name": "mpw-tauri",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "dev": "concurrently \"npm --prefix MPW run dev\" \"tauri dev\" --kill-others --success first"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^1",
    "concurrently": "^9.2.1"
  }
}
